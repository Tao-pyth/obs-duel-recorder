# v1.1.8 OBS Smoke Evidence

Date: 2026-05-31

Scope: real `C:\Program Files\obs-studio` install/load smoke for the `v1.1.8`
package.

This smoke validates package layout, Plugin load, Dock registration, Worker
health, upload readiness, and the v1.1.8 Dock UI visibility corrections.

## Package Under Test

- Package: `build/release-v1.1.8/obs-duel-recorder-v1.1.8.zip`
- ZIP SHA256:

```text
F1DAF94D0F09DF249AB3F02F4B6E91BC6AA17171E86FC02A03674AD3E3ACDDD2
```

Plugin DLL SHA256:

```text
CC994965E471B179AD4EC2CD691C1CBF64F9F355B7D344524023327B4AE96D01
```

Worker EXE SHA256:

```text
B9CCD48F0228B0B618A81A649825E723816B360C2656F85F7AA44B2C961E7269
```

## Install Validation

Real OBS install target:

```text
C:\Program Files\obs-studio
```

Verifier result:

```text
OK: OBS Duel Recorder install layout looks valid.
```

The installed Plugin DLL and Worker executable hashes matched the package:

```text
C:\Program Files\obs-studio\obs-plugins\64bit\obs-duel-recorder.dll
C:\Program Files\obs-studio\obs-plugins\worker\odr-worker\odr-worker.exe
```

The known wrong Worker placement was absent:

```text
C:\Program Files\obs-studio\obs-plugins\64bit\worker
```

## Real OBS Load Smoke

OBS executable:

```text
C:\Program Files\obs-studio\bin\64bit\obs64.exe
```

Runtime processes:

```text
obs64.exe responding=true
odr-worker.exe responding=true
```

Worker health probe:

```text
status=ok
worker_version=2.3.0
worker_api_version=2.3
schema_version=10
```

Upload status probe:

```text
readiness_state=client_secret_missing
ready_upload_count=5
manual_review_count=0
provider_default=mock
google_dependencies_available=true
```

OBS log:

```text
C:\Users\takuy\AppData\Roaming\obs-studio\logs\2026-05-31 11-09-57.txt
```

Relevant entries:

```text
OBS Duel Recorder plugin startup
OBS Duel Recorder frontend ready
OBS Duel Recorder dock registered id=obs-duel-recorder-status
OBS Duel Recorder worker preflight host=127.0.0.1 port=8787 user_data_dir=C:\Users\takuy\AppData\Roaming\obs-duel-recorder\user_data
OBS Duel Recorder worker reused existing instance api_version=2.3 version=2.3.0
OBS Duel Recorder heartbeat baseline interval_ms=2000 failure_threshold=3 api_version=2.3 version=2.3.0
OBS Duel Recorder overlay update applied field=deck_name
OBS Duel Recorder overlay update applied field=sequence_number
OBS Duel Recorder overlay update applied field=result
OBS Duel Recorder overlay update applied field=opponent_deck
OBS Duel Recorder overlay update applied field=recording_state
```

## Dock UI Visual Smoke

Screenshots were saved locally under:

```text
build/obs-smoke-v1.1.8/
```

Captured files:

```text
obs-v1.1.8-record-tab.png
obs-v1.1.8-upload-tab.png
obs-v1.1.8-template-tab.png
obs-v1.1.8-manage-tab.png
obs-v1.1.8-main-window.png
```

Visual checks:

- Record tab action buttons are arranged in two rows and labels are readable.
- Record tab no longer shows the duplicate upload-text preview button.
- Representative frame selectors show `1枚目`, `2枚目`, and `3枚目` without arrow icons.
- Upload tab actions are arranged in two columns and labels are readable.
- Template tab is present and contains upload title, description, tags, and preview controls.
- Manage tab uses inline settings and Save Settings. The duplicate Open Settings button is absent.

## Notes

- The first OBS launch stopped at the unclean shutdown sentinel. The
  `.sentinel` directory was backed up under the OBS config directory before the
  normal smoke run.
- OBS standard module warnings for AJA, DeckLink, AMF, and an existing WASAPI
  source were present. They were unrelated to OBS Duel Recorder and did not
  prevent Plugin load, Dock registration, Worker health, or overlay updates.

初心者向けに言うと、この確認は「ファイルが正しい場所に入っているか」「OBSがPluginを読み込めるか」「Workerが動くか」「実際のDockで今回直したボタンやタブが見えるか」を分けて確認した記録です。
