# v1.1.1 Validation Report

This report records #450 validation for the `v1.1.1` UI and documentation hardening work coordinated by #440.

Validation date: 2026-05-26 JST

## Passed Checks

| Check | Command | Result |
|---|---|---|
| Worker unit tests | `python -m unittest discover -s app\worker\tests -p "test_*.py"` | Passed: 97 tests |
| Markdown links | `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File docs\tools\validate_markdown_links.ps1` | Passed: 134 files |
| Japanese user docs coverage | `python scripts\validate_jp_user_docs_coverage.py --root .` | Passed |
| Pages artifact build | `python scripts\build_docs_site.py --root . --output build\docs-site` | Passed |
| Whitespace | `git diff --check` | Passed; only normal LF-to-CRLF working-copy warnings |
| UI image XML parse | PowerShell XML parse over `docs\user\assets\v1.1.1-*.svg` | Passed: 3 SVG files |
| Release package build and validation with existing local artifacts | `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File scripts\build_release_package.ps1 -Version v1.1.1 -PluginDllPath build\plugin\Release\obs-duel-recorder.dll -WorkerBundlePath build\worker\odr-worker -OutputDir build\release -Clean` | Passed package validation |
| Final Plugin build from current source | `cmake -S app/plugin -B build/plugin-v1.1.1-final-clean4 ...` then `cmake --build build/plugin-v1.1.1-final-clean4 --config Release` | Passed |
| Final release package with rebuilt Plugin DLL | `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File scripts\build_release_package.ps1 -Version v1.1.1 -PluginDllPath build\plugin-v1.1.1-final-clean4\Release\obs-duel-recorder.dll -WorkerBundlePath build\worker\odr-worker -OutputDir build\release-v1.1.1-final -Clean` | Passed package validation |

## Package Evidence

Local validation package:

```text
build/release-v1.1.1-final/obs-duel-recorder-v1.1.1.zip
```

SHA256:

```text
95D5C5336F544AB68F68B7D24117544D1912542BE08EDDA73A0FB734A3BE4A9A
```

Checksum file:

```text
build/release-v1.1.1-final/SHA256SUMS.txt
```

Rebuilt Plugin DLL:

```text
build/plugin-v1.1.1-final-clean4/Release/obs-duel-recorder.dll
```

Plugin DLL SHA256:

```text
5E24C7B3740E9D714AE885E1DFC93123C0699DB066A17912AFF9E00F9CC790EF
```

## Environment Resolution

The first validation run could not find CMake on PATH. The terminal build environment was later established with:

- CMake 3.31.8
- Visual Studio Build Tools 2022 17.14 / MSVC 19.44.35227
- Qt 6.8.3 `msvc2022_64`
- OBS Studio 32.1.2 portable runtime and OBS source/build CMake package paths

The Codex process also had duplicate `PATH` and `Path` variables, so the successful build used a clean child-process environment and a workspace-local temp directory for MSBuild.

## Release Impact

Final publication is no longer blocked by local Plugin build availability. The v1.1.1 release ZIP was rebuilt with the rebuilt Plugin DLL and passed package validation.
