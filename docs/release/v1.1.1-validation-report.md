# v1.1.1 Validation Report

This report records #450 validation for the `v1.1.1` UI and documentation hardening work coordinated by #440.

Validation date: 2026-05-26 JST

## Passed Checks

| Check | Command | Result |
|---|---|---|
| Worker unit tests | `python -m unittest discover -s app\worker\tests -p "test_*.py"` | Passed: 97 tests |
| Markdown links | `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File docs\tools\validate_markdown_links.ps1` | Passed: 134 files |
| Japanese user docs coverage | `python scripts\validate_jp_user_docs_coverage.py --root .` | Passed |
| Pages artifact build | `python scripts\build_docs_site.py --root . --output build\docs-site` | Passed |
| Whitespace | `git diff --check` | Passed; only normal LF-to-CRLF working-copy warnings |
| UI image XML parse | PowerShell XML parse over `docs\user\assets\v1.1.1-*.svg` | Passed: 3 SVG files |
| Release package build and validation with existing local artifacts | `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File scripts\build_release_package.ps1 -Version v1.1.1 -PluginDllPath build\plugin\Release\obs-duel-recorder.dll -WorkerBundlePath build\worker\odr-worker -OutputDir build\release -Clean` | Passed package validation |

## Package Evidence

Local validation package:

```text
build/release/obs-duel-recorder-v1.1.1.zip
```

SHA256:

```text
4660DD36DDE2AB243F1086B20501D76ED110999BBA825F95BBEEE125296E2E42
```

Checksum file:

```text
build/release/SHA256SUMS.txt
```

## Environment Blockers

Plugin build was attempted by checking CMake availability:

```powershell
cmake --version
```

Result:

```text
cmake is not available on PATH in this local environment.
```

This is a local environment blocker. It does not prove product failure, but it prevents this workstation from rebuilding `obs-duel-recorder.dll` from the current `main` commit.

## Release Impact

The local release package validation passed, but the package used an existing `build/plugin/Release/obs-duel-recorder.dll` artifact. Because the Plugin DLL could not be rebuilt locally after the v1.1.1 Dock UI, theme, Help, and automatic setup changes, this package is validation evidence only.

#451 must not publish final v1.1.1 release assets unless one of these is true:

- a current Plugin DLL is rebuilt from the final v1.1.1 commit on a workstation with CMake, OBS, Qt, and Visual Studio prerequisites, then the package is rebuilt and validated, or
- the maintainer explicitly accepts and records a publication blocker/handoff instead of publication.
