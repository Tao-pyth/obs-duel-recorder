# v1.1.4 OBS Smoke Evidence

Date: 2026-05-30

Scope: isolated OBS smoke and real `C:\Program Files\obs-studio` install/load
smoke for the `v1.1.4` package generated from this branch.

This smoke validates package layout, Plugin load, Dock registration, Worker
launch, Worker health, and correct Worker placement.

## Package Under Test

- Package: `build/release-v1.1.4/obs-duel-recorder-v1.1.4.zip`
- ZIP SHA256:

```text
513525137740FB35F480270DE85CD5332A255C89008D18C2517715DC5A4500D5
```

Plugin DLL SHA256:

```text
8CD89870E9395135FB0B8A838419C2BCE89070F7DD69180EE3A38362A129879A
```

The package Plugin DLL and real OBS installed Plugin DLL hashes matched.

## Install Validation

Install into an isolated OBS root:

```powershell
powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File scripts\install_release_package.ps1 -PackageRoot build\release-v1.1.4\obs-duel-recorder-v1.1.4 -ObsRoot build\obs-smoke-v1.1.4\obs-studio
```

Result:

```text
OK: OBS Duel Recorder install layout looks valid.
OK: Install/update assistant finished. Restart OBS before testing the Dock.
```

The real OBS install target was also verified:

```text
C:\Program Files\obs-studio\obs-plugins\64bit\obs-duel-recorder.dll
```

Its SHA256 matched the package Plugin DLL hash above.

## Real OBS Load Smoke

OBS executable:

```text
C:\Program Files\obs-studio\bin\64bit\obs64.exe
```

Worker health probe:

```text
worker_health=ok
worker_version=2.3.0
worker_api_version=2.3
```

OBS log:

```text
C:\Users\takuy\AppData\Roaming\obs-studio\logs\2026-05-30 00-21-02.txt
```

Relevant entries:

```text
OBS Duel Recorder plugin startup
OBS Duel Recorder frontend ready
OBS Duel Recorder dock registered id=obs-duel-recorder-status
OBS Duel Recorder worker preflight host=127.0.0.1 port=8787 user_data_dir=C:\Users\takuy\AppData\Roaming\obs-duel-recorder\user_data
OBS Duel Recorder worker launch attempt command_path=C:\Program Files\obs-studio\obs-plugins\worker\odr-worker\odr-worker.exe args="--host 127.0.0.1 --port 8787"
OBS Duel Recorder worker spawned pid=23960 command_path=C:\Program Files\obs-studio\obs-plugins\worker\odr-worker\odr-worker.exe
OBS Duel Recorder worker running api_version=2.3 version=2.3.0
OBS Duel Recorder heartbeat baseline interval_ms=2000 failure_threshold=3 api_version=2.3 version=2.3.0
```

The OBS log search found no matching OBS Duel Recorder entries for:

- `failed_to_start`
- `worker_launch action_required`
- `wrong_nested`
- `api_incompatible`

## Notes

- Runtime `user_data` was not modified by package generation.
- Real OBS started the packaged Worker from
  `obs-plugins\worker\odr-worker\odr-worker.exe`.
- The install assistant now requests UAC elevation when updating a protected
  OBS root under `Program Files`.
